# 项目开发规范（AI出题读书法）— 2026-08-26 固化

> 来源：本次 333 评审复盘（部署 3 次 / 测试脚本 4 版 / 统计口径绕弯 / 改码引入回归）。
> 原则：这些坑业界都有成熟解法，遵守即可，不自己发明。详细方法论见全局 skill `test-first`（先写测试）。
> **执行方式：规范靠工具强制（CI），不靠自觉**——push 时 GitHub Actions 自动跑检查，不过就不部署（见 `.github/workflows/deploy.yml`）。文档只是说明，强制靠机器。

## 1. 测试先行（test-first）— 解决"测试脚本反复改"

**when**：改任何功能逻辑、修任何 bug 之前。
**do**：
- 先写一个**会失败**的测试（复现 bug 或描述期望行为）→ 跑，看到它失败（红）→ 确认失败点就是目标问题 → 再改实现 → 跑，变绿。
- 测试**测行为不测实现**：输入 X 得输出 Y、边界（空/单个/超长/零）、副作用。不测内部调用。
- 断言要具体（`toEqual({...})` 而不是 `toBeTruthy()`）。
- 一个测试一件事，名字说清测什么。
- **判断测试是否有效：把实现回退掉，测试会不会失败？** 不会 = 白写。

## 2. 数据模型先设计透 — 解决"统计口径绕弯、同题多记录"

**when**：新功能涉及数据存储/统计之前。
**do**：动手前先用文字/表格说清三件事，写进任务计划：
- **存什么**：每条记录有哪些字段（id/setId/qIndex/answer/correct/supplement/aiComment/ts）
- **怎么算**：统计口径（本例教训：每题只保留最新一条作答，答对从错题集移除——"最新一次"而非"累加所有"）
- **怎么变**：增删改规则（每题覆盖旧记录；答对→错题移除；删除题集连带删记录）
- 说清后再写码；说不清就先问，不猜。

## 3. 修改后必验证 — 解决"改码引入致命回归"

**when**：任何修改（含改测试脚本本身）后、交付前。
**do**：
- 改完跑针对性验证（不是只跑 E2E，还要跑单元级：解析函数、统计函数）
- 交付前跑全量回归套件（E2E + 离线 + 上传 + 规则本地 + 明细）
- 验证方式要真实：模拟真实入口（浏览器实测），不绕过（这次教训：curl 拼完整路径误报 200，实际页面引用根路径 404）

## 4. 部署自动化 — 已落地

- 本项目已配置 GitHub Actions 自动部署（`.github/workflows/deploy.yml`，peaceiris 方案）。
- 改完代码：`git push origin main` → 自动上线。**不要手动推 gh-pages**。
- 网络：本机走代理时必须显式给命令设 `HTTPS_PROXY`/`HTTP_PROXY=http://127.0.0.1:7897`（gh/git 不读系统代理）；推送用 gh 凭据：
  `git -c http.proxy=http://127.0.0.1:7897 -c credential.helper="!$GH auth git-credential" push`
- 改了 `.github/workflows/` 需要 workflow 权限（已授权）。
