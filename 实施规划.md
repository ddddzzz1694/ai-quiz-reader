# AI出题读书法 · 未实现功能实施规划 v1

> 状态：规划（等老板确认后实施）｜2026-08-26
> 用途：剩余 13 项功能的实施蓝图——每项"怎么实现、flash 能不能做、验收标准、推进机制"
> 模型约束：当前用 deepseek-v4-flash，**单次任务必须小、可验证、可中断恢复**（见 §四）

---

## 〇、当前基础（规划依据）

- 代码：纯前端 PWA，`static/js/app.js`（1248 行）+ `index.html`（186 行）+ `style.css`（360 行）
- 存储：IndexedDB 6 表——settings（设置）/ sets（题集）/ records（作答）/ chats（追问）/ transfers（远迁移）/ prompt_history（规则版本）
- 已有机制：CI 自动测试+部署（push 即上线）、pre-commit 钩子、test-first 规范、手机 UI 实测脚本
- **每个新功能 = 加表或加字段 + 加几个函数 + 加 HTML/样式 + 加一个测试**，模式高度统一，flash 完全可做

---

## 一、剩余功能清单 + 实现方式（每项具体到改哪）

### 第二批（4+1 项，刷题闭环）

**B1 多本书管理**
- 存储：新增 `books` 表 `{id, title, desc, createdAt}`；`sets` 加字段 `bookId`
- 界面：首页顶部加"我的书"入口 → 书列表页（新建书/改名）→ 点书看该书题集；生成题目时可选"归入哪本书"
- 函数：`renderBooks()` / `createBook()` / `renderBookSets(bookId)`；改 `onGenerate` 归书
- 测试：建书→出题归书→切书看对应题集→刷新保留
- flash 可做：✅ 纯前端 CRUD，无复杂逻辑

**B2 错题本独立页**
- 存储：不改表（用现有 `records`）
- 界面：数据页加"❌ 错题本"入口 → 跨所有题集汇总"每题最新一条答错"的题 → 按书/题集分组 → 支持"重练（进入刷题）"
- 函数：`renderWrongBook()`（复用 `calcSetStats` 的"最新一条"口径）
- 测试：答错 2 题→错题本见 2 题→重练答对→错题本变 0
- flash 可做：✅ 汇总逻辑简单

**B3 每日目标**
- 存储：`settings` 加 `dailyGoal`（默认 0=不设）
- 界面：设置页加"每天刷 N 题"；首页显示今日进度（今日已答 X/N）；达标弹鼓励语
- 函数：`getTodayCount()`（records 按天聚合）/ `renderDailyGoal()`
- 测试：设目标 3→答 3 题→达标提示→第二天清零
- flash 可做：✅

**B4 统计看板**
- 存储：不改表（用 `records`）
- 界面：数据页加"📊 统计"入口 → 正确率趋势（近7天折线）、总刷题天数、每本书掌握度（答对率）
- 函数：`renderStats()`（聚合 records，按天/按书分组）
- 测试：造多条记录→统计数字正确→刷新保留
- flash 可做：✅（趋势图用纯 CSS/内联 SVG，不引图表库）

**B5 导出增强（Markdown/纯文本）**
- 界面：`onExport` 加格式选择（JSON 默认 / Markdown / 纯文本）
- 函数：`exportMarkdown()`（题+解析+我的作答拼 Markdown）/ `exportPlainText()`
- 测试：导出 Markdown→内容含题目和解析→下载文件名正确
- flash 可做：✅ 字符串拼接

### 第三批（8 项）

**C1 编辑权（删题/改题/加笔记）**
- 界面：答题明细弹层每题加"✏️ 编辑"→ 改题干/选项/解析/删题（confirm 确认）；"📝 笔记"→ 存 `records.note`
- 函数：`editQuestion()` / `deleteQuestion()` / `addNote()`
- 测试：改题→重刷显示新题→删题→题数减1→笔记保留
- flash 可做：✅

**C2 版本管理（多套题选主版本）**
- 界面：题集"重新生成"不覆盖原题集，另存新题集（标题加"v2"）；题集列表可标"⭐ 主版本"
- 存储：`sets` 加 `isMain`、`parentId`
- 测试：重新生成→两套共存→标主版本→切换
- flash 可做：✅

**C3 参考错题出题**
- 逻辑：出题时把"当前错题的知识点"拼进 prompt，让 AI 出变体题/补解析（只增不删）
- 函数：改 `callAI` 的 user 消息加错题信息
- 测试：mock 验证 prompt 含错题信息（不真调 AI）
- flash 可做：✅（只是 prompt 拼接）

**C4 间隔复习**
- 存储：`sets` 加 `nextReviewAt`；答题后按间隔（1/3/7/15天）更新
- 界面：首页加"🔁 待复习 N 题"入口 → 列出到期题 → 进入复习刷题
- 测试：答完→nextReviewAt=明天→改时间→显示待复习
- flash 可做：✅

**C5 费曼自检**
- 界面：完成页加"🗣️ 用自己的话讲核心"→ 弹输入框 → AI 对比原题判断掌握度（复用 `runTransferCheck` 模式）
- 函数：`openFeynmanCheck()`
- 测试：按钮存在→弹层→无Key降级
- flash 可做：✅

**C6 碎碎念/日记**
- 存储：新增 `murmurs` 表 `{id, text, tags, ts}`
- 界面：首页加"💭 碎碎念"入口 → 时间线+标签+导出
- 测试：写一条→时间线显示→刷新保留→导出
- flash 可做：✅

**C7 复习提醒 / 成就 / 字体 / 夜间（体验包）**
- 复习提醒：`settings.dailyReminderTime`，到点首页提示（PWA 通知后续）
- 成就：`settings.achievements`，连续打卡/掌握度达标给徽章
- 字体：`settings.fontScale`，字号调节（CSS 变量）
- 夜间：`settings.darkMode`，暗色主题（CSS 变量切换）
- 每个都是"设置项 + 一处 UI 生效"，flash 可做：✅

**C8 EPUB 上传**
- 拿来主义：用 `epub.js`（开源解析库，现成）解析 EPUB → 提取文本 → 填输入框
- 界面：上传按钮加"EPUB"选项
- 测试：造一个最小 EPUB→上传→文本提取
- flash 可做：✅（引库不改库）

### 后置：云端同步（老板拍板后置，规划留档）
- 方案：Supabase 免费账号（注册一次）+ 登录后数据同步（sets/records/books 上传下载）
- 依赖：账号体系，先不做

---

## 二、验收标准（每项通用 + 专项）

**通用（每项必过）**：
- [ ] 功能可真实操作（不是摆设）
- [ ] 数据刷新后保留、断网可读（IndexedDB）
- [ ] 手机 UI 实测无横向溢出、按钮 ≥48px
- [ ] JS 语法通过 + 新增测试 PASS + 原有测试不回归
- [ ] CI 自动部署成功、线上可访问

**专项**：见每项"测试"栏。

---

## 三、推进机制（全套流程，固定动作）

**每项功能按此循环（S1→S7）**：
- S1 规划（本次已做）：出实施规划，确认每项实现方式
- S2 逐项实施：**一项做完再下一项，不并行**
- S3 每项写完：跑专项测试 + 原有回归测试（本地）
- S4 手机实测：Pixel 5 视口跑 UI 检查
- S5 提交推送：`git commit`（pre-commit 自动检查）→ `push`（CI 自动测试+部署）
- S6 线上验证：curl 200 + 关键文件
- S7 汇报老板：改了啥/验证结果/让老板试

**批次节奏**：
- 第二批 5 项做完 → 老板试用确认 → 再开第三批
- 每 2-3 项让老板试一次（吸取"一次 8 项感觉差一点"教训）

**中断恢复**：
- 每项做完 git commit，中断后看 `git log` + 本规划文档就知道进度
- 状态写本文件（勾选完成项），不靠记忆

---

## 四、flash 模型能不能做——结论

**全部能做**。理由：
1. 所有功能都是"加表/加字段 + 加几个纯前端函数 + 加 HTML + 加一个测试"，模式统一，无复杂算法
2. 每个功能单文件改动 ≤150 行，单次任务小，flash 上下文够用
3. 唯一的"AI 能力"（出题/点评/追问/远迁移/费曼）都是调用 DeepSeek API 返回文本，app 只做拼接和展示，不要求模型本地推理
4. 已配 test-first + CI + pre-commit，flash 每步有机器验证，不需要"一次想对全部"

**flash 做不了的（已规避）**：不需要长链路规划（已拆成单项）、不需要复杂图形（统计用简单折线）、不需要本地 AI（用 API）。

---

## 五、风险与注意

- EPUB 解析库要验证浏览器兼容（GitHub Pages 子路径相对引用）
- 统计看板折线图不要引第三方库（纯 SVG，避免子路径加载问题）
- 多本书改造要兼容旧数据（现有题集 bookId 为空 → 归入"未分类"）
- 云端同步后置，不要提前做（账号体系工作量超出功能本身）
